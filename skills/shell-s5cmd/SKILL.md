---
name: shell-s5cmd
description: "Install and use s5cmd. TIL note about shell. Use when working with shell and the user mentions s5cmd or related topics."
---

# Install and use s5cmd

`s5cmd` is a parallel S3 and local filesystem execution tool

Install:
```
brew install peak/tap/s5cmd
```

Ref: [peak/s5cmd](https://github.com/peak/s5cmd)
