---
name: shell-lsof-port
description: "Find the processes listening on port. TIL note about shell. Use when working with shell and the user mentions lsof port or related topics."
---

# Find the processes listening on port

Find the processes listening on a given port number using the `lsof` command.

```
lsof -i :<port>
```
